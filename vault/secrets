# Local secrets — one KEY=VALUE per line. Each KEY becomes an environment variable
# when you load this file (see vault/README.md).
#
#   - Lines starting with '#' and blank lines are ignored.
#   - Everything after the FIRST '=' is the value (values may contain '=').
#   - Surrounding single/double quotes are stripped.
#
# NOTE: this file is intentionally TRACKED in git (NOT gitignored) — the value
# below is a throwaway local demo credential, deliberately committed for this
# take-home. Do NOT follow this pattern for real secrets.

DB_USER=postgres
DB_PASSWORD=5a101ea0ef414355bd543a079b02827c
